import React from "react";
import { ActivityIndicator, StyleSheet, View } from "react-native";

const Loading = () => {
  return (
    <View>
      <ActivityIndicator size="large" color="#f9de4b" />
    </View>
  );
};

export default Loading;
